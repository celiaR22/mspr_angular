import { StatusPlantColorPipe } from './status-plant-color.pipe';

describe('StatusPlantColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusPlantColorPipe();
    expect(pipe).toBeTruthy();
  });
});
